# context.dev API key (starts with ctxt_secret_)
CONTEXT_DEV_API_KEY=
# Site location for weather lookups
SITE_LOCATION=Dubai Marina
# Site coordinates — used by the primary weather source (Open-Meteo)
SITE_LAT=25.080
SITE_LON=55.140
# Optional: JSONL file for the question log (learn list survives restarts)
# ASK_LOG_PATH=/var/lib/heatsafe/ask-log.jsonl
